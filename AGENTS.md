# Retired source - do not author here

Use `tim-builds/timbuilds.dev` for all current website work. Both main domains are built from that repository's main branch through Cloudflare Pages.

This repository/worktree is historical and may be archived. Do not force-push, recreate the mirror procedure, overwrite the canonical CNAME, or merge its older content into the live site. Preserve local unfinished work and open a branch against the canonical source instead.

Read the current canonical AGENTS.md, README.md and HOSTING.md. Keep original private games, credentials, operator notes and test evidence outside public commits. Native OpenHoops implementation and real-account/device acceptance remain separately owned.

Historical Pages settings are retained for a specifically approved rollback. Unarchive before an emergency edit; do not delete this repository or its branches.
